<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="ypsilon.app.cdn"
      android:versionName="3.0_alfa1" android:versionCode="3">
    <uses-sdk android:minSdkVersion="6" />  
    <application
    	android:icon="@drawable/ic_rc_timer"
    	android:label="@string/app_name"
		android:allowBackup="true"
    	android:theme="@android:style/Theme.Holo.NoActionBar">
        <activity android:name=".Control"
                  android:label="@string/app_name" android:screenOrientation="portrait" android:icon="@drawable/ic_rc_timer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    	<service
    		android:name="CountService"
    		android:process=":counter"	
    		android:icon="@drawable/ic_rc_timer">
    	</service>

	</application>
</manifest>